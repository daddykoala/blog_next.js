
//diference entre une app react et une app nextjs
//nextjs est une app react avec des fonctionnalités en plus
//nextjs va creer des pages statiques pour chaque page de notre app.
//difference entre le rendu statique et le rendu serveur
//le rendu statique est le rendu qui se fait coté client
//
//le rendu serveur est le rendu qui se fait coté serveur
export default function index() {
  return (
    <div>le blog
        
        
    </div>
  )
}
